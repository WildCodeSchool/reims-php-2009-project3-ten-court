{% extends 'base.html.twig' %}

{% block title %}TennisMatch{% endblock %}

{% block body %}
<div class="backgroundDescriptionContainer">
    
        <div class="matchDescriptionContainer"> 
            <h1>{{ tennis_match.name }}</h1>  
                <div class="descriptionRow date">
                    <p>du </p>
                    <p>{{ tennis_match.startHour ? tennis_match.startHour|date('d-m-Y H:i') : '' }}</p>
                </div>
                <div class="descriptionRow date">
                    <p>au </p>
                    <p>{{ tennis_match.endHour ? tennis_match.endHour|date('d-m-Y H:i') : '' }}</p>
                </div>
                <div class="descriptionRow">
                    <p>Lieu: </p>
                    <p>{{ tennis_match.adress }}</p>
                </div>
                <div class="descriptionRow">
                    <p>Description</p>
                    <p>{{ tennis_match.description }}</p>
                </div>
        </div>
    <div class="button">
        <a class="back" href="{{ path('search_matchs') }}">retour à la liste</a>
        <a class="participate" href="{{ path('tennis_match_edit', {'id': tennis_match.id}) }}">participer</a>
    </div>

<<<<<<< HEAD
   
</div>
{{ include('tennis_match/_delete_form.html.twig') }}
=======
    <table class="table">
        <tbody>
            <tr>
                <th>Date du match</th>
                <td>{{ tennis_match.eventDate ? tennis_match.eventDate|date('d-m-Y') : ''}}</td>
            </tr>
            <tr>
                <th>StartHour</th>
                <td>{{ tennis_match.startHour ? tennis_match.startHour|date('H:i') : '' }}</td>
            </tr>
            <tr>
                <th>EndHour</th>
                <td>{{ tennis_match.endHour ? tennis_match.endHour|date('H:i') : '' }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tennis_match.name }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ tennis_match.adress }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ tennis_match.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tennis_match_index') }}">back to list</a>

    <a href="{{ path('tennis_match_edit', {'id': tennis_match.id}) }}">edit</a>

    {{ include('tennis_match/_delete_form.html.twig') }}
>>>>>>> dff6bcc87ff50008d617a4aecc837bd5373b325a
{% endblock %}
