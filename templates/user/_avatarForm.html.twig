<div class="editionForm">
    <h1 class="editionTitleForm">Modifier mon avatar</h1>
    {{ form_start(formAvatar) }}
    <div class="containerEditionForm">
        <div class="editionItemForm">
            {{ form_row(formAvatar.avatar) }}
        </div>
        <div class="editionItemForm">
            {{ form_row(formAvatar.update) }}
        </div>
    {{ form_end(formAvatar) }}
        <a class="backButtonEditProfile" href="{{ path('user_profile', {'slug': user.slug}) }}">Retour</a>
        <a class="EditAccueilButton" href="{{ path('user_index') }}">Accueil</a>
        {# <a class="DeleteButtonEditProfile" href="{{ path('user_delete', {'id': user.id}) }}">Supprimer</a> #}
    </div>
</div>

